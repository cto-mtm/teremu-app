rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the Cloud Functions API (Admin SDK bypasses
    // rules). Clients have no Firebase SDK, so lock direct access down.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
