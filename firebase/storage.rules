rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Receipt images are written and served exclusively by the Cloud
    // Functions API (Admin SDK). No direct client access.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
